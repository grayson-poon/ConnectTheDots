<!DOCTYPE html>
<html>
  <head>
    <title>ConnectTheDots</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= favicon_link_tag asset_path('favicon.png') %>

    <% if current_user %>
      <script>
        window.currentUserId = <%= current_user.id %>
      </script>
    <% end %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>

