<!DOCTYPE html>
<html>
  <head>
    <title>ConnectTheDots</title>
    <meta property="og:title" content="ConnectTheDots" />
    <meta property="og:description" content="ConnectTheDots App" />
    <meta property="og:type" content="Website" />
    <meta property="og:image" content="<%= image_url('thumbnail.jpg') %>" />
    <meta property="og:url" content="https://connect-the-dots-grayson-poon.herokuapp.com/#/" />
    <meta name="author" content="Grayson Poon" />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= favicon_link_tag asset_path('favicon.png') %>

    <% if current_user %>
      <script>
        window.currentUserId = <%= current_user.id %>
      </script>
    <% end %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>

