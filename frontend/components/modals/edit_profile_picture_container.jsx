import { connect } from "react-redux";
import EditProfilePictureModal from "./edit_profile_picture";

const mSTP = (state, ownProps) => ({

});

const mDTP = (dispatch) => ({
  
});

export default connect(mSTP, mDTP)(EditProfilePictureModal);